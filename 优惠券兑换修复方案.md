# 优惠券兑换失败 - 最终修复方案

> **修复时间**: 2025-11-07 15:30 (UTC+8)
> **问题**: 优惠券兑换失败，显示"資料驗證失敗，請稍後再試"

---

## 🎯 问题根源分析

### **为什么 EVoucher 兑换成功，Coupon 兑换失败？**

#### **数据库结构差异**

**Coupon 表：**
```sql
-- DEFAULT 约束
DF_Coupon_UsedTime: DEFAULT (sysutcdatetime())

-- CHECK 约束（关键！）
CK_Coupon_UsedFields:
  ([IsUsed]=0 AND [UsedTime] IS NULL AND [UsedInOrderID] IS NULL)
  OR
  ([IsUsed]=1 AND [UsedTime] IS NOT NULL AND [UsedInOrderID] IS NOT NULL)
```

**EVoucher 表：**
```sql
-- DEFAULT 约束
DF_EVoucher_UsedTime: DEFAULT (sysutcdatetime())

-- CHECK 约束
(无相关 CHECK 约束！)
```

### **失败流程**

**Coupon 兑换时：**
```
1. 代码设置: IsUsed=false, UsedTime=null, UsedInOrderId=null
2. EF Core 发送 INSERT 语句
3. 数据库发现 UsedTime=null，触发 DEFAULT 约束
4. 数据库自动设置: UsedTime = sysutcdatetime() (例如: 2025-11-07 15:30:00)
5. 数据库检查 CK_Coupon_UsedFields 约束
6. 约束验证失败: IsUsed=0 但 UsedTime != NULL (违反约束)
7. ❌ 抛出异常: "The INSERT statement conflicted with the CHECK constraint"
```

**EVoucher 兑换时：**
```
1. 代码设置: IsUsed=false, UsedTime=null
2. EF Core 发送 INSERT 语句
3. 数据库发现 UsedTime=null，触发 DEFAULT 约束
4. 数据库自动设置: UsedTime = sysutcdatetime()
5. 数据库没有相关 CHECK 约束
6. ✅ 插入成功（即使 IsUsed=false 且 UsedTime != NULL）
```

---

## ✅ 修复方案（不跨区）

### **方案说明**

在 `WalletService.cs` 的 `ExchangeForCouponAsync` 方法中，添加 EF Core 的强制设置逻辑，确保 NULL 值被明确发送到数据库，而不是依赖 DEFAULT 约束。

### **修改文件**

`GamiPort\GamiPort\Areas\MiniGame\Services\WalletService.cs`

**第 671-676 行（关键修复）：**

```csharp
var entry = _context.Coupons.Add(coupon);

// 關鍵修復：強制設置這些屬性的當前值為 null
// 確保 EF Core 將 NULL 發送到數據庫，而不是依賴 DEFAULT 約束
entry.Property(e => e.UsedTime).CurrentValue = null;
entry.Property(e => e.UsedInOrderId).CurrentValue = null;
```

### **工作原理**

1. `_context.Coupons.Add(coupon)` 将实体添加到 EF Core 上下文
2. `entry.Property(e => e.UsedTime).CurrentValue = null` 强制设置当前值
3. EF Core 生成的 SQL 语句会显式指定 `UsedTime = NULL`（而不是省略该字段让数据库使用 DEFAULT）
4. 数据库接收到显式的 NULL 值，不触发 DEFAULT 约束
5. CHECK 约束验证通过：`IsUsed=0` 且 `UsedTime IS NULL`
6. ✅ 插入成功

### **对比：修复前后的 SQL 语句**

**修复前（推测）：**
```sql
INSERT INTO [Coupon]
([CouponCode], [CouponTypeId], [UserId], [IsUsed], [AcquiredTime], [IsDeleted])
VALUES
('CPN-2511-ABC123', 1, 10000001, 0, '2025-11-07 15:30:00', 0);
-- 注意：UsedTime 和 UsedInOrderId 没有出现在 INSERT 语句中
-- 数据库会自动应用 DEFAULT 约束
```

**修复后（预期）：**
```sql
INSERT INTO [Coupon]
([CouponCode], [CouponTypeId], [UserId], [IsUsed], [AcquiredTime], [UsedTime], [UsedInOrderId], [IsDeleted])
VALUES
('CPN-2511-ABC123', 1, 10000001, 0, '2025-11-07 15:30:00', NULL, NULL, 0);
-- 注意：UsedTime 和 UsedInOrderId 显式设置为 NULL
-- 数据库不会应用 DEFAULT 约束
```

---

## 🧪 测试步骤

### **步骤 1: 启动应用**

```bash
cd "C:\Users\n2029\Desktop\work-1105\GamiPort\GamiPort"
dotnet run
```

### **步骤 2: 访问兑换页面**

打开浏览器访问：
```
https://localhost:7160/MiniGame/Wallet/Exchange
```

### **步骤 3: 测试兑换功能**

1. 选择任意一个优惠券类型（例如"全站85折"）
2. 设置数量为 1
3. 点击"立即兑换"按钮

### **步骤 4: 预期结果**

**成功场景：**
- ✅ 显示绿色成功提示："成功兌換 1 張優惠券「全站85折」"
- ✅ 页面点数自动减少
- ✅ 可以在"我的优惠券"页面看到新兑换的优惠券

**如果仍然失败：**
- ❌ 显示红色错误提示："資料驗證失敗，請聯繫客服"
- 需要进一步诊断（查看后端日志）

### **步骤 5: 数据库验证**

成功兑换后，检查数据库：

```sql
-- 查看最新插入的优惠券
SELECT TOP 5
    CouponID,
    CouponCode,
    IsUsed,
    UsedTime,
    UsedInOrderID,
    AcquiredTime
FROM Coupon
ORDER BY CouponID DESC;

-- 预期结果：
-- IsUsed = 0
-- UsedTime = NULL (关键！)
-- UsedInOrderID = NULL (关键！)
```

### **步骤 6: 日志验证**

查看应用日志，应该看到：

```
info: GamiPort.Areas.MiniGame.Services.WalletService[0]
      開始兌換優惠券: UserId=xxx, CouponTypeId=1, Quantity=1

dbug: GamiPort.Areas.MiniGame.Services.WalletService[0]
      生成優惠券代碼: CPN-2511-XXXXXXXX, Iteration=1

dbug: GamiPort.Areas.MiniGame.Services.WalletService[0]
      優惠券對象創建成功: CouponCode=CPN-2511-XXXXXXXX, IsUsed=False, UsedTime=NULL

info: GamiPort.Areas.MiniGame.Services.WalletService[0]
      成功兌換優惠券: UserId=xxx, CouponTypeId=1, Quantity=1, PointsUsed=1000
```

**如果看到错误：**
```
fail: GamiPort.Areas.MiniGame.Services.WalletService[0]
      資料庫更新失敗: UserId=xxx, CouponTypeId=1, InnerException=...
```

复制完整错误信息并提供给开发者分析。

---

## 📊 其他修复内容

除了核心的 `CurrentValue` 修复，本次还包含以下增强：

### **1. 增强错误处理**

**WalletService.cs 第 693-722 行：**

```csharp
catch (DbUpdateException dbEx)
{
    _logger.LogError(dbEx, "資料庫更新失敗: UserId={UserId}, CouponTypeId={CouponTypeId}, InnerException={InnerException}",
        userId, couponTypeId, dbEx.InnerException?.Message ?? "無");

    // 檢查是否為唯一性約束違反
    if (dbEx.InnerException?.Message?.Contains("UNIQUE") == true ||
        dbEx.InnerException?.Message?.Contains("duplicate") == true)
    {
        return (false, "優惠券代碼生成衝突，請重試", new List<string>());
    }

    // 檢查是否為CHECK約束違反
    if (dbEx.InnerException?.Message?.Contains("CHECK") == true ||
        dbEx.InnerException?.Message?.Contains("CK_") == true)
    {
        return (false, "資料驗證失敗，請聯繫客服", new List<string>());
    }

    return (false, $"兌換失敗：{dbEx.InnerException?.Message ?? dbEx.Message}", new List<string>());
}
```

**优点：**
- 区分不同类型的数据库错误
- 返回更具体的错误信息
- 便于诊断问题

### **2. 详细日志记录**

**WalletService.cs 第 597-598 行：**
```csharp
_logger.LogInformation("開始兌換優惠券: UserId={UserId}, CouponTypeId={CouponTypeId}, Quantity={Quantity}",
    userId, couponTypeId, quantity);
```

**WalletService.cs 第 658 行：**
```csharp
_logger.LogDebug("生成優惠券代碼: {CouponCode}, Iteration={Iteration}", couponCode, i + 1);
```

**WalletService.cs 第 680-681 行：**
```csharp
_logger.LogDebug("優惠券對象創建成功: CouponCode={CouponCode}, IsUsed={IsUsed}, UsedTime={UsedTime}",
    couponCode, coupon.IsUsed, coupon.UsedTime == null ? "NULL" : coupon.UsedTime.ToString());
```

### **3. 前端调试增强**

**Exchange.cshtml 第 236-244 行：**
```javascript
console.log('🔍 CSRF Token Container:', document.getElementById('antiForgeryTokenContainer'));
console.log('🔍 CSRF Token Input:', tokenInput);
console.log('🔍 CSRF Token Value:', tokenInput ? tokenInput.value : 'NULL');
console.log('✅ CSRF Token獲取成功，準備發送請求...');
```

**Exchange.cshtml 第 245-252 行：**
```javascript
console.log('📤 發送請求:', {
    url: '@Url.Action("ExchangeForCoupon", "Wallet")',
    method: 'POST',
    couponId: couponId,
    quantity: quantity,
    bodyLength: requestBody.length
});
```

**Exchange.cshtml 第 261-270 行：**
```javascript
console.log('📥 收到響應:', {
    status: response.status,
    statusText: response.statusText,
    ok: response.ok
});
console.log('📋 響應數據:', data);
```

---

## 🔍 故障排查

### **如果修复后仍然失败**

#### **场景 1: 仍然显示"資料驗證失敗"**

**可能原因**：EF Core 的 `CurrentValue` 设置没有生效

**诊断步骤**：
1. 开启 EF Core SQL 日志：
   ```json
   // appsettings.Development.json
   {
     "Logging": {
       "LogLevel": {
         "Microsoft.EntityFrameworkCore.Database.Command": "Information"
       }
     }
   }
   ```

2. 重启应用，尝试兑换

3. 查看日志中的 SQL 语句，确认是否包含 `UsedTime = NULL`

**预期 SQL（修复成功）：**
```sql
INSERT INTO [Coupon] (..., [UsedTime], [UsedInOrderId], ...)
VALUES (..., NULL, NULL, ...);
```

**错误 SQL（修复失败）：**
```sql
INSERT INTO [Coupon] (...) -- UsedTime 和 UsedInOrderId 缺失
VALUES (...);
```

#### **场景 2: 显示其他错误信息**

根据错误信息类型：

- **"優惠券代碼生成衝突"** → UNIQUE 约束违反，重试即可
- **"點數不足"** → 用户点数不够，充值后再试
- **"優惠券類型不存在"** → CouponTypeId 无效，检查数据库
- **"兌換失敗：..."** → 查看后面的详细错误信息

#### **场景 3: 无任何响应或超时**

- 检查网络请求是否到达后端（F12 → Network 标签）
- 检查应用是否正常运行（命令行窗口是否有异常）
- 检查数据库连接是否正常

---

## 📝 技术说明

### **为什么不直接删除数据库的 DEFAULT 约束？**

虽然删除 `DF_Coupon_UsedTime` DEFAULT 约束是最彻底的解决方案，但：

1. **需要修改数据库架构**：这超出了"不跨区"的限制
2. **可能影响其他代码**：其他地方可能依赖这个 DEFAULT 约束
3. **需要协调后台**：GameSpace 项目可能也使用这个表

本次修复方案是**纯代码层面的解决方案**，不修改数据库，完全在 `GamiPort\Areas\MiniGame` 范围内。

### **为什么 EVoucher 不需要类似修复？**

EVoucher 表虽然也有 `DF_EVoucher_UsedTime` DEFAULT 约束，但**没有相关的 CHECK 约束**。

即使数据库自动填充了 `UsedTime = sysutcdatetime()`，也不会违反任何约束，所以插入成功。

---

## 📦 修改文件清单

### **代码修改（1个文件）**

- `GamiPort/GamiPort/Areas/MiniGame/Services/WalletService.cs`
  - 第 597-598 行：添加开始日志
  - 第 658 行：添加优惠券代码生成日志
  - 第 671-676 行：**核心修复 - CurrentValue 设置**
  - 第 680-681 行：添加对象创建日志
  - 第 693-722 行：增强异常处理

- `GamiPort/GamiPort/Areas/MiniGame/Views/Wallet/Exchange.cshtml`
  - 第 236-244 行：添加 CSRF Token 调试日志
  - 第 245-252 行：添加请求发送日志
  - 第 261-270 行：添加响应接收日志

### **文档（2个文件）**

- `优惠券兑换失败诊断指南.md`（上次创建）
- `优惠券兑换修复方案.md`（本文件）

---

## ✅ 预期效果

**修复前：**
```
用户点击"立即兑换"
→ 后端尝试插入 Coupon
→ 数据库触发 DF_Coupon_UsedTime DEFAULT
→ 数据库检查 CK_Coupon_UsedFields 约束
→ ❌ 约束违反，插入失败
→ 前端显示"資料驗證失敗，請稍後再試"
```

**修复后：**
```
用户点击"立即兑换"
→ 后端尝试插入 Coupon
→ EF Core 强制设置 UsedTime = NULL
→ 数据库接收显式 NULL 值，不触发 DEFAULT
→ 数据库检查 CK_Coupon_UsedFields 约束
→ ✅ 约束通过（IsUsed=0 且 UsedTime IS NULL）
→ 插入成功
→ 前端显示"成功兌換 1 張優惠券..."
```

---

## 🎓 总结

**问题本质**：数据库的 DEFAULT 约束与 CHECK 约束冲突

**解决方案**：使用 EF Core 的 `entry.Property().CurrentValue` 强制设置 NULL 值

**修改范围**：仅 `GamiPort\Areas\MiniGame` 内，不跨区

**风险评估**：低（仅影响优惠券兑换功能，不影响其他功能）

**测试重点**：验证兑换后数据库中的 `UsedTime` 和 `UsedInOrderId` 字段确实是 NULL

---

**修复完成时间**: 2025-11-07 15:35 (UTC+8)
**修复人员**: Claude Code
**文档版本**: v1.0
