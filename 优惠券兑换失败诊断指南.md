# 优惠券兑换失败 - 完整诊断与修复指南

> **创建时间**: 2025-11-07 15:00 (UTC+8)
> **问题**: 用户点击"立即兑换"后显示"兑换失败，请稍后再试"

---

## 📋 目录

1. [已完成的修复](#已完成的修复)
2. [三大可能原因](#三大可能原因)
3. [诊断步骤](#诊断步骤)
4. [测试方法](#测试方法)
5. [如何阅读日志](#如何阅读日志)
6. [常见问题FAQ](#常见问题faq)

---

## ✅ 已完成的修复

### 1. **增强错误处理与日志记录**

**修改文件**: `Services/WalletService.cs`

**修复内容**:
- ✅ 添加方法开始日志，记录所有输入参数
- ✅ 添加优惠券代码生成日志
- ✅ 添加优惠券对象创建日志
- ✅ 分离 `DbUpdateException` 和通用异常处理
- ✅ 识别 UNIQUE 约束冲突
- ✅ 识别 CHECK 约束冲突
- ✅ 返回详细错误信息给前端

**关键代码片段**:
```csharp
// 开始日志
_logger.LogInformation("開始兌換優惠券: UserId={UserId}, CouponTypeId={CouponTypeId}, Quantity={Quantity}",
    userId, couponTypeId, quantity);

// 生成代码日志
_logger.LogDebug("生成優惠券代碼: {CouponCode}, Iteration={Iteration}", couponCode, i + 1);

// 数据库异常处理
catch (DbUpdateException dbEx)
{
    _logger.LogError(dbEx, "資料庫更新失敗: UserId={UserId}, CouponTypeId={CouponTypeId}, InnerException={InnerException}",
        userId, couponTypeId, dbEx.InnerException?.Message ?? "無");

    // 检查UNIQUE约束
    if (dbEx.InnerException?.Message?.Contains("UNIQUE") == true)
        return (false, "優惠券代碼生成衝突，請重試", new List<string>());

    // 检查CHECK约束
    if (dbEx.InnerException?.Message?.Contains("CHECK") == true)
        return (false, "資料驗證失敗，請聯繫客服", new List<string>());

    return (false, $"兌換失敗：{dbEx.InnerException?.Message ?? dbEx.Message}", new List<string>());
}
```

---

### 2. **前端调试日志增强**

**修改文件**: `Views/Wallet/Exchange.cshtml`

**修复内容**:
- ✅ 添加 CSRF Token 调试日志（检查 token 容器、input 元素、token 值）
- ✅ 添加请求发送日志（URL、方法、参数）
- ✅ 添加响应接收日志（状态码、响应数据）
- ✅ 添加错误详情日志

**如何使用**:
1. 打开浏览器开发者工具（F12）
2. 切换到 Console 标签
3. 点击"立即兑换"按钮
4. 观察控制台输出的日志信息

**预期日志流程**:
```
🔍 CSRF Token Container: <div id="antiForgeryTokenContainer">...</div>
🔍 CSRF Token Input: <input name="__RequestVerificationToken" value="...">
🔍 CSRF Token Value: CfDJ8...
✅ CSRF Token获取成功，准备发送请求...
📤 发送请求: {url: "/MiniGame/Wallet/ExchangeForCoupon", method: "POST", couponId: 1, quantity: 1, bodyLength: 234}
📥 收到响应: {status: 200, statusText: "OK", ok: true}
📋 响应数据: {success: false, message: "兌換失敗：..."}
❌ 兌換失敗: 兌換失敗：...
```

---

## 🔍 三大可能原因

### **原因1: 数据库 CHECK 约束冲突（最可能）**

**约束名称**: `CK_Coupon_UsedFields`

**约束定义**:
```sql
([IsUsed] = 0 AND [UsedTime] IS NULL AND [UsedInOrderID] IS NULL)
OR
([IsUsed] = 1 AND [UsedTime] IS NOT NULL AND [UsedInOrderID] IS NOT NULL)
```

**问题描述**:
- 数据库的 `Coupon` 表有一个 `DF_Coupon_UsedTime` 默认约束，默认值为 `sysutcdatetime()`
- 当创建新优惠券时，如果 `UsedTime` 字段触发了默认约束（自动填充当前时间），而 `IsUsed=0`，就会违反 CHECK 约束
- 虽然代码中显式设置了 `UsedTime = null`，但需要验证 EF Core 是否正确传递 NULL 值

**修复状态**: ✅ 已修复（代码中显式设置 `UsedTime = null` 和 `UsedInOrderId = null`，并添加注释）

**验证方法**:
```sql
-- 如果日志显示 CHECK 约束错误，执行此查询查看约束详情
SELECT
    tc.CONSTRAINT_NAME,
    cc.CHECK_CLAUSE
FROM INFORMATION_SCHEMA.TABLE_CONSTRAINTS tc
JOIN INFORMATION_SCHEMA.CHECK_CONSTRAINTS cc
    ON tc.CONSTRAINT_NAME = cc.CONSTRAINT_NAME
WHERE tc.TABLE_NAME = 'Coupon' AND tc.CONSTRAINT_NAME = 'CK_Coupon_UsedFields';
```

---

### **原因2: 优惠券代码 UNIQUE 约束冲突**

**约束名称**: `UQ_Coupon_CouponCode`

**约束定义**: `CouponCode` 字段必须全局唯一

**问题描述**:
- `GenerateUniqueCouponCodeAsync` 方法使用 GUID 前8位生成代码
- 格式: `CPN-YYMM-XXXXXXXX` (8位随机字符)
- 虽然有10次重试机制，但在高并发或极端情况下可能失败
- 当前数据库中已有约1500个 CPN- 格式优惠券，冲突概率仍然很低

**修复状态**: ⚠️ 部分修复（已添加 DbUpdateException 处理，识别 UNIQUE 冲突）

**验证方法**:
```sql
-- 检查是否有重复的 CouponCode
SELECT CouponCode, COUNT(*) as Count
FROM Coupon
GROUP BY CouponCode
HAVING COUNT(*) > 1;
-- 应返回 0 行
```

---

### **原因3: CSRF Token 验证失败**

**问题描述**:
- CSRF Token 未正确生成到页面中
- CSRF Token 在传递过程中丢失
- ASP.NET Core 的 `[ValidateAntiForgeryToken]` 拒绝了请求

**修复状态**: ✅ 已修复（上次会话已改用 visible div 容器，本次添加详细日志）

**验证方法**:
- 查看浏览器控制台日志
- 如果看到 "❌ CSRF Token not found!" 或 "系統錯誤：安全驗證失敗"，则确认是此问题
- 如果网络请求返回 400 Bad Request，查看响应内容是否提到 "antiforgery"

---

## 🧪 诊断步骤

### **步骤1: 启动应用程序（开启详细日志）**

```bash
cd "C:\Users\n2029\Desktop\work-1105\GamiPort\GamiPort"

# 方法1: 使用环境变量开启详细日志
$env:ASPNETCORE_ENVIRONMENT="Development"
$env:Logging__LogLevel__Default="Debug"
$env:Logging__LogLevel__Microsoft.EntityFrameworkCore="Information"
dotnet run

# 方法2: 修改 appsettings.json 后直接运行
# 在 appsettings.json 中设置:
# "Logging": {
#   "LogLevel": {
#     "Default": "Debug",
#     "Microsoft.EntityFrameworkCore": "Information"
#   }
# }
dotnet run
```

**预期输出**: 应用启动后显示监听地址，例如:
```
info: Microsoft.Hosting.Lifetime[14]
      Now listening on: https://localhost:7160
info: Microsoft.Hosting.Lifetime[14]
      Now listening on: http://localhost:5001
```

---

### **步骤2: 打开浏览器开发者工具**

1. 访问 `https://localhost:7160/MiniGame/Wallet/Exchange`
2. 按 `F12` 打开开发者工具
3. 切换到 **Console** 标签

---

### **步骤3: 尝试兑换优惠券**

1. 选择任意一个优惠券类型（例如"全站85折"）
2. 设置数量为 1
3. 点击"立即兑换"按钮
4. **同时观察两处日志**:
   - **浏览器控制台**（前端日志）
   - **命令行窗口**（后端日志）

---

### **步骤4: 分析前端日志**

**场景A: CSRF Token 问题**
```
🔍 CSRF Token Container: null
🔍 CSRF Token Input: null
🔍 CSRF Token Value: NULL
❌ CSRF Token not found!
```
👉 **解决方案**: 检查 `Exchange.cshtml` 第10-12行，确认 `<div id="antiForgeryTokenContainer">` 存在且包含 `@Html.AntiForgeryToken()`

---

**场景B: 网络请求失败**
```
✅ CSRF Token获取成功，准备发送请求...
📤 發送請求: {url: "/MiniGame/Wallet/ExchangeForCoupon", method: "POST", ...}
📥 收到響應: {status: 400, statusText: "Bad Request", ok: false}
❌ 請求錯誤: SyntaxError: Unexpected token < in JSON at position 0
```
👉 **解决方案**:
- 状态码 400: 可能是 CSRF token 验证失败或参数格式错误
- 状态码 500: 服务器内部错误，需查看后端日志
- JSON 解析错误: 响应不是 JSON 格式，可能返回了 HTML 错误页面

---

**场景C: 业务逻辑失败**
```
✅ CSRF Token获取成功，准备发送请求...
📤 發送請求: {url: "/MiniGame/Wallet/ExchangeForCoupon", method: "POST", ...}
📥 收到響應: {status: 200, statusText: "OK", ok: true}
📋 響應數據: {success: false, message: "兌換失敗：Violation of UNIQUE KEY constraint..."}
❌ 兌換失敗: 兌換失敗：Violation of UNIQUE KEY constraint...
```
👉 **解决方案**: 查看后端日志获取完整错误信息

---

### **步骤5: 分析后端日志**

**场景A: CHECK 约束错误**
```
info: GamiPort.Areas.MiniGame.Services.WalletService[0]
      開始兌換優惠券: UserId=10000001, CouponTypeId=1, Quantity=1

dbug: GamiPort.Areas.MiniGame.Services.WalletService[0]
      生成優惠券代碼: CPN-2511-A1B2C3D4, Iteration=1

dbug: GamiPort.Areas.MiniGame.Services.WalletService[0]
      優惠券對象創建成功: CouponCode=CPN-2511-A1B2C3D4, IsUsed=False, UsedTime=NULL

fail: GamiPort.Areas.MiniGame.Services.WalletService[0]
      資料庫更新失敗: UserId=10000001, CouponTypeId=1, InnerException=The INSERT statement conflicted with the CHECK constraint "CK_Coupon_UsedFields"...
```

👉 **诊断**: CHECK 约束冲突，说明 `UsedTime` 或 `UsedInOrderID` 字段值不符合约束条件

👉 **解决方案**:
```sql
-- 检查默认约束定义
SELECT OBJECT_DEFINITION(OBJECT_ID('DF_Coupon_UsedTime'));
-- 如果返回 (sysutcdatetime())，需要删除此默认约束

-- 删除默认约束（需要在 pause-and-ask mode 征求用户同意）
ALTER TABLE Coupon DROP CONSTRAINT DF_Coupon_UsedTime;

-- 或者修改 EF Core 配置，确保显式传递 NULL
```

---

**场景B: UNIQUE 约束错误**
```
info: GamiPort.Areas.MiniGame.Services.WalletService[0]
      開始兌換優惠券: UserId=10000001, CouponTypeId=1, Quantity=1

dbug: GamiPort.Areas.MiniGame.Services.WalletService[0]
      生成優惠券代碼: CPN-2511-A1B2C3D4, Iteration=1

fail: GamiPort.Areas.MiniGame.Services.WalletService[0]
      資料庫更新失敗: UserId=10000001, CouponTypeId=1, InnerException=Violation of UNIQUE KEY constraint 'UQ_Coupon_CouponCode'. Cannot insert duplicate key in object 'dbo.Coupon'. The duplicate key value is (CPN-2511-A1B2C3D4).
```

👉 **诊断**: 优惠券代码重复

👉 **解决方案**:
```csharp
// 增加随机字符长度（从8位增加到12位）
var uniqueId = Guid.NewGuid().ToString("N")[..12].ToUpper();

// 或者添加更多重试次数
int maxRetries = 20; // 从10增加到20
```

---

**场景C: 外键约束错误**
```
fail: GamiPort.Areas.MiniGame.Services.WalletService[0]
      資料庫更新失敗: UserId=10000001, CouponTypeId=999, InnerException=The INSERT statement conflicted with the FOREIGN KEY constraint "FK_Coupon_CouponType"...
```

👉 **诊断**: `CouponTypeId` 不存在或已被删除

👉 **解决方案**:
```sql
-- 检查 CouponType 是否存在
SELECT * FROM CouponType WHERE CouponTypeId = 999 AND IsDeleted = 0;
```

---

**场景D: 点数不足**
```
info: GamiPort.Areas.MiniGame.Services.WalletService[0]
      開始兌換優惠券: UserId=10000001, CouponTypeId=1, Quantity=1
-- 没有数据库错误日志
```

👉 **诊断**: 业务逻辑验证失败（点数不足、类型不存在等），没有到达数据库插入步骤

👉 **解决方案**: 查看前端返回的 `message` 字段

---

## 🧾 如何阅读日志

### **日志级别说明**

| 级别 | 前缀 | 含义 | 示例 |
|-----|------|-----|------|
| dbug | Debug | 详细调试信息 | 生成的优惠券代码 |
| info | Information | 一般信息 | 开始兑换优惠券 |
| warn | Warning | 警告信息 | 优惠券类型不存在 |
| fail | Error | 错误信息 | 数据库更新失败 |

### **关键日志搜索关键字**

```bash
# 搜索兑换相关日志
"開始兌換優惠券"
"資料庫更新失敗"
"兌換優惠券失敗"
"成功兌換優惠券"

# 搜索异常类型
"DbUpdateException"
"UNIQUE"
"CHECK"
"FOREIGN KEY"

# 搜索用户操作
"UserId=10000001"  # 替换为实际用户ID
"CouponTypeId=1"   # 替换为实际类型ID
```

---

## ❓ 常见问题 FAQ

### **Q1: 浏览器控制台没有日志输出？**

**A**: 检查以下几点:
1. 确认浏览器开发者工具已打开（F12）
2. 确认已切换到 Console 标签
3. 检查是否有 console 过滤器启用（清除所有过滤）
4. 刷新页面后重新尝试兑换

---

### **Q2: 命令行窗口没有日志输出？**

**A**: 检查日志级别配置:
```json
// appsettings.json 或 appsettings.Development.json
{
  "Logging": {
    "LogLevel": {
      "Default": "Debug",  // 改为 Debug
      "GamiPort.Areas.MiniGame": "Debug",  // 添加此行
      "Microsoft.EntityFrameworkCore": "Information"  // 添加此行以查看 SQL
    }
  }
}
```

---

### **Q3: 如何查看实际执行的 SQL 语句？**

**A**:
```json
// appsettings.Development.json
{
  "Logging": {
    "LogLevel": {
      "Microsoft.EntityFrameworkCore.Database.Command": "Information"
    }
  }
}
```

重启应用后，日志会显示:
```
info: Microsoft.EntityFrameworkCore.Database.Command[20101]
      Executed DbCommand (5ms) [Parameters=[@p0='CPN-2511-ABC123', @p1='1', @p2='10000001', ...], CommandType='Text', CommandTimeout='30']
      INSERT INTO [Coupon] ([CouponCode], [CouponTypeId], [UserId], [IsUsed], [AcquiredTime], [UsedTime], [UsedInOrderId], [IsDeleted])
      VALUES (@p0, @p1, @p2, @p3, @p4, @p5, @p6, @p7);
```

---

### **Q4: 如何确认是哪个约束导致的失败？**

**A**: 查看 `InnerException` 字段:
- 包含 `UNIQUE KEY constraint 'UQ_Coupon_CouponCode'` → CouponCode 重复
- 包含 `CHECK constraint 'CK_Coupon_UsedFields'` → UsedTime/IsUsed 字段冲突
- 包含 `CHECK constraint 'CK_Coupon_IsUsed'` → IsUsed 字段值不是 0 或 1
- 包含 `FOREIGN KEY constraint 'FK_Coupon_CouponType'` → CouponTypeId 不存在
- 包含 `FOREIGN KEY constraint 'FK_Coupon_Users'` → UserId 不存在

---

### **Q5: 修复后仍然失败，下一步该怎么办？**

**A**: 收集完整诊断信息:
1. 前端完整控制台日志（截图或复制文本）
2. 后端完整日志（从 "開始兌換優惠券" 到 "失敗" 的所有行）
3. 网络请求详情（F12 → Network 标签 → 找到 ExchangeForCoupon 请求 → 查看 Headers、Payload、Response）
4. 数据库约束检查结果:
   ```sql
   -- 复制此查询结果
   SELECT
       tc.CONSTRAINT_NAME,
       tc.CONSTRAINT_TYPE,
       cc.CHECK_CLAUSE
   FROM INFORMATION_SCHEMA.TABLE_CONSTRAINTS tc
   LEFT JOIN INFORMATION_SCHEMA.CHECK_CONSTRAINTS cc
       ON tc.CONSTRAINT_NAME = cc.CONSTRAINT_NAME
   WHERE tc.TABLE_NAME = 'Coupon';
   ```

---

## 🛠️ 紧急修复方案

### **如果确认是 CHECK 约束问题（DF_Coupon_UsedTime 默认值冲突）**

⚠️ **需要修改数据库架构，必须征求用户同意！**

```sql
-- 方案1: 删除 UsedTime 默认约束（推荐）
ALTER TABLE Coupon DROP CONSTRAINT DF_Coupon_UsedTime;

-- 方案2: 修改默认约束为 NULL（如果方案1失败）
ALTER TABLE Coupon DROP CONSTRAINT DF_Coupon_UsedTime;
ALTER TABLE Coupon ADD CONSTRAINT DF_Coupon_UsedTime DEFAULT NULL FOR UsedTime;
```

**执行后验证**:
```sql
-- 检查默认约束是否已删除
SELECT
    OBJECT_NAME(c.default_object_id) AS DefaultConstraintName,
    c.name AS ColumnName
FROM sys.columns c
WHERE c.object_id = OBJECT_ID('Coupon') AND c.name = 'UsedTime';
-- 应返回 DefaultConstraintName = NULL
```

---

### **如果确认是 UNIQUE 约束问题（CouponCode 冲突频繁）**

修改 `Services/WalletService.cs` 中的 `GenerateUniqueCouponCodeAsync` 方法:

```csharp
// 增加随机字符长度
var uniqueId = Guid.NewGuid().ToString("N")[..12].ToUpper(); // 从8位增加到12位

// 增加重试次数
int maxRetries = 20; // 从10增加到20

// 添加时间戳后缀（终极方案）
var timestamp = DateTimeOffset.UtcNow.ToUnixTimeMilliseconds();
var code = $"CPN-{yearMonth}-{uniqueId}-{timestamp}";
```

---

## 📝 测试清单

完成修复后，按以下清单逐项测试:

- [ ] **测试1**: 兑换 1 张优惠券（类型1: 免运券）
- [ ] **测试2**: 兑换 5 张优惠券（类型2: 全站85折）
- [ ] **测试3**: 兑换 10 张优惠券（类型3: 满$500折$50）
- [ ] **测试4**: 点数不足时兑换（应显示"点数不足"）
- [ ] **测试5**: 兑换数量超过100（应显示"兑换数量必须在 1-100 之间"）
- [ ] **测试6**: 检查数据库 `Coupon` 表是否正确插入记录
- [ ] **测试7**: 检查 `User_Wallet` 表点数是否正确扣除
- [ ] **测试8**: 检查 `WalletHistory` 表是否记录交易
- [ ] **测试9**: 多个标签页同时兑换（测试并发）
- [ ] **测试10**: 刷新页面后重新兑换（测试 CSRF token 重新生成）

---

## 📞 联系与支持

如果按照本指南操作后仍然无法解决问题，请提供:
1. ✅ 前端控制台完整日志
2. ✅ 后端命令行完整日志
3. ✅ 网络请求详情（Headers + Response）
4. ✅ 数据库约束查询结果

---

**文档维护**: Claude Code
**最后更新**: 2025-11-07 15:00 (UTC+8)
